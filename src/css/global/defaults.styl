*
  box-sizing border-box
  margin 0
  padding 0

  &::after,
  &::before
    box-sizing border-box

:root
  font-size 20px

body
  color graphite
  font-family fira sans
  font-weight 100
  line-height 1.5
